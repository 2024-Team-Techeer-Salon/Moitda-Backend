input {
  beats {
      port => 5044
  }
}

filter {
  # 필요에 따라 필터를 추가합니다.
  # 예시:
  # json {
  #   source => "message"
  # }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "logstash-%{+YYYY.MM.dd}"
  }
  stdout {
    codec => rubydebug
  }
}